# YouTube 只读真实验证 · 2026-07-14

> 历史快照：本文保留 2026-07-14 当时的缺权限结论；2026-07-15 已用隔离 token 完成真实评论与可核验空结果，见 [`后续补证`](2026-07-15-youtube-comment-validation.md)。

## 范围

本次验证现有 YouTube 凭据、既有公开视频、Day 1 产品内关联链和数据读取，不上传、不修改平台内容。
频道名、频道编号、平台视频编号和原始响应只保存在本机忽略目录，不进入公开仓库。

## 关联链

| 环节 | 脱敏证据 |
|---|---|
| 原始内容 | 一份 16,976 字节的课程逐字稿；SHA-256 `e90b53eb3965c3837d559af905458f430f5dd2ec9477461187142b472264b14c` |
| 成片 | 一份 2,466,600,072 字节的 MP4；SHA-256 `e78c6c781b0c5f829a24e845875865441ba851c2e651e1a9c3cff55aa4629233` |
| 发布记录 | 对应公开视频编号的 SHA-256 为 `d4e4c1f5067d6a8483f03193d3a3810e19cb85af420259c812ddbbe304b65926` |
| 数据快照 | 2026-07-14T14:56:35Z 读取到播放 75、点赞 1、平台评论计数 1 |

逐字稿、成片与平台编号来自同一条既有业务记录。原文件和原始平台响应不随开源包分发。

## 输入、输出与失败

1. 使用系统 Python 启动桥接脚本时失败，返回 `ModuleNotFoundError`。
2. 改用现有 uploader 自带的隔离 Python 环境后，频道只读校验成功，并且恰好返回一个授权频道。
3. 对既有公开视频执行基础数据读取成功，返回发布时间、公开状态和统计字段。
4. 评论列表请求返回平台 `403 insufficientPermissions`。对活动 access token 的 scope 做脱敏核验后确认：上传 token 有 `youtube` 与 `youtube.upload`，但没有评论接口要求的 `youtube.force-ssl`。连接器没有伪造空评论，也没有让整次指标同步失败。

## Day 1 产品内闭环

2026-07-14T15:25:51Z 使用 Day 1 自己的 HTTP 合同重新建立一条本地脱敏记录，依次完成：

1. 创建账号组和 YouTube 账号记录，并通过 `/accounts/{id}/inspect` 校验唯一授权频道；
2. 把真实逐字稿作为 Context 创建视频，再从同一内容导入脚本和自动补齐的第一版分镜；
3. 只登记既有 MP4 的大小、校验值与本机忽略路径，不复制 2.46GB 文件；
4. 把既有公开视频作为 `imported` Publication 关联到该视频；
5. 通过 `/publications/{id}/sync` 保存一条带获取时间和 YouTube 原始统计字段的 MetricSnapshot；
6. 将评论正文不可读取的原因保存在对应 Publication warning，同时保留平台评论计数；
7. 从同一工作库成功导出 JSON 和 CSV。

产品内结果：1 个 Context 来源、1 个脚本版本、1 个分镜版本、1 个成片记录、1 个既有发布记录和 1 个真实数据快照。公开记录不含频道名、频道编号、视频编号、标题、原始响应或本机路径。

后续回归把评论权限改为上传 token 与评论 token 分离：未配置独立评论授权时，适配器能力声明为不可读评论并保存“缺少 `youtube.force-ssl`”的明确原因；频道、发布和指标继续工作。再次从 Day 1 `/sync` 成功保存第 2 个真实 MetricSnapshot，全程没有上传、修改平台内容或改写 uploader token。

## 2026-07-15 外部动作预检

- 上传工具、隔离 Python、上传 token 和 OAuth client secrets 均在位；未输出任何凭据内容。
- 上传 token 仍只含 `youtube` 和 `youtube.upload`，独立评论授权输出不存在，因此未触发 OAuth。
- OAuth client 已脱敏确认为 installed app，client ID、client secret 和本机回调均存在；该检查只返回布尔值，没有输出实际凭据。
- 上传 token 和 client secrets 的本机权限已从 `0644` 收紧为 `0600`；只改文件权限，未修改文件内容。
- Day 1 YouTube 适配器已固化该安全边界：每次调用前收紧凭据权限，子进程使用 `077` 文件掩码，私有描述与原始证据使用 `0600`。
- 执行了上传工具自带的 `--dry-run`：只读频道校验通过，有 `DRY RUN` 完成标记，隐私级别为 `private`，进程退出码为 0。该分支在创建 `videos.insert` 请求前返回，未上传视频、未创建平台内容。

## 能证明与不能证明

- 能证明：现有凭据可在非交互模式下校验频道；基础视频指标真实可读；评论不可读时有可核验原因；Day 1 产品能把真实既有内容、成片、发布记录和数据保存在同一条视频链上。
- 不能证明：Day 01 工作台已经执行过新的真实上传。真实发布仍是不可逆动作，必须在界面中明确确认后才能验收。
- 不能证明：评论正文已回流。需要用户显式运行独立评论授权命令并同意 `youtube.force-ssl` 后再验收；现有上传 token 不会被修改。
- 不能证明：YouTube 提供成交数据；Day 01 不把样例成交数字冒充 YouTube 回流。
