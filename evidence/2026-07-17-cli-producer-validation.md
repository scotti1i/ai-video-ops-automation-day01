# 2026-07-17 · 本机 CLI 写脚本引擎真实验证

## 验证内容

在本机（已安装并登录 `claude` 命令）通过 Day 1 自己的写脚本合同（`ScriptProducer.produce`）真实调用 Claude Code 命令行生成一条脚本，验证 `claude-cli` 引擎端到端可用：提示词组装 → 子进程调用 `claude -p … --output-format json` → JSON 信封解析 → `GeneratedPlan` 校验。

## 输入（公开样例，无脱敏需要）

- Context：`商品：便携榨汁杯。卖点：随行杯直接饮用、杯体可拆洗、USB-C 充电。目标人群：通勤上班族。`
- 指令：`为通勤人群写一条 25 秒竖屏带货短视频脚本，痛点开场，中文口播`

## 输出摘要（真实返回，未修饰）

- 6 个镜头，钩子/卖点/痛点/证明/异议/行动结构完整
- 首句台词：「天天赶地铁，多久没喝过鲜榨果汁了？」
- 台词全部为可拍摄的口语；只引用了提供的三个卖点（同杯直饮、可拆洗、USB-C），未编造效果、价格或优惠
- 解析为合法 `GeneratedPlan`，与内置模板、OpenAI 适配器走同一条后续链路（版本、质量检查、视频记录不变）

## 结论与边界

- `claude-cli` 引擎：**已真实验证**（本机一次真实生成）。
- `codex-cli` 引擎：本机检测到已安装，管线与 `claude-cli` 共用同一套子进程与解析实现，单测覆盖噪音提取/超时/坏输出，但**尚未做过一次真实生成**——按「没有真实证据不标已打通」的规矩，连接页仅显示「已安装」。
- 演示模式不受影响，恒用内置示例模板；CLI 不可用时启动日志提示并回退，不中断使用。
